import React, { useEffect, useState } from "react";
import API from "../utils/API";
import SearchEmployee from "./SearchEmployee";

// return 100 people, name, phone number, email, DOB,
// add search bar at the top

// table, table row, table content
// will need thead to wrap around first tr
// <thead>
// <tr>
// will need tbody to wrap around everything else
// as you type in search filters

// array of people filter "onChange" to only include what the user searches for
// need to ABC, sorting method

// search container
// employee container - table
// to be called in APP.js

// App.js will have onchange function
// employee array will need to be passed and sorted

// useEffect & useState

export default EmployeeDetail;
